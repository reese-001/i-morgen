version: 0.2

env:
  variables:
    SOURCE_ARTIFACTS_PATH="source-artifacts/"

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - pip install -r requirements.txt
  build:
    commands:
      - python menu_script.py



